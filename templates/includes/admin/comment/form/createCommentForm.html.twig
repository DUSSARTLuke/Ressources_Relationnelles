{{ form_start(form) }}
<div class="container">
    <div class="mb-3 row">
        {{ form_row(form.user) }}
    </div>
    <div class="row">
        <label for="">Commentaire</label>
        {{ form_widget(form.content,{ 'attr': {'rows': '6'} }) }}
    </div>
    <div class="row">
        {{ form_row(form.save) }}
    </div>
    <br>
</div>
{{ form_end(form) }}

